---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: zuul
resources:
  - store-init-job.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: build-node-store-init
    files:
      - flake.nix
      - flake.lock
      - store-init.sh
