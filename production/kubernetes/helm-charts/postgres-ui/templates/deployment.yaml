apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "postgres-ui.fullname" . }}
  labels:
    {{- include "postgres-ui.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "postgres-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "postgres-ui.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
