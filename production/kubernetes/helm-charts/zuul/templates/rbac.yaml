---
# nxs serviceAccount values cannot safely express this fixed ClusterRoleBinding:
# it binds the namespace default ServiceAccount to the cluster-admin ClusterRole.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: zuul-default-cluster-admin
  labels:
    {{- include "zuul.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: default
    namespace: zuul
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
