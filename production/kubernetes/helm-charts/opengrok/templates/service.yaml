apiVersion: v1
kind: Service
metadata:
  name: {{ include "opengrok.fullname" . }}
  labels:
    {{- include "opengrok.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opengrok.selectorLabels" . | nindent 4 }}
