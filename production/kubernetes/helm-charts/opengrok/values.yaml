---
nxs:
  releasePrefix: "-"

  generic:
    podSecurityContext:
      fsGroup: 1111
    usePredefinedAffinity: false

  serviceAccount:
    opengrok:
      automountServiceAccountToken: true

  pvcs:
    opengrok-data-local-pvc:
      accessModes:
        - ReadWriteOnce
      size: 5Gi
      storageClassName: local-rebuildable
    opengrok-src-local-pvc:
      accessModes:
        - ReadWriteOnce
      size: 5Gi
      storageClassName: local-rebuildable

  deployments:
    opengrok:
      replicas: 1
      serviceAccountName: opengrok
      automountServiceAccountToken: true
      containers:
        opengrok:
          image: opengrok/docker
          imageTag: latest
          imagePullPolicy: Always
          env:
            - name: SYNC_PERIOD_MINUTES
              value: "1440"
            - name: WORKERS
              value: "1"
            - name: CATALINA_OPTS
              value: "-Xms128m -Xmx512m"
            - name: INDEXER_JAVA_OPTS
              value: "-Xms128m -Xmx768m"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: reindex
              containerPort: 5000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 640Mi
            limits:
              cpu: "2"
              memory: 1Gi
          volumeMounts:
            - name: data
              mountPath: /opengrok/data
            - name: src
              mountPath: /opengrok/src
            - name: ssh
              mountPath: /home/appuser/.ssh
              readOnly: true
      volumes:
        - name: data
          type: pvc
          originalName: opengrok-data-local-pvc
        - name: src
          type: pvc
          originalName: opengrok-src-local-pvc
      extraVolumes:
        - name: ssh
          secret:
            secretName: opengrok-ssh  # pragma: allowlist secret
            defaultMode: 0400

  services:
    opengrok:
      type: ClusterIP
      ports:
        - name: http
          port: 8080
          targetPort: http
          protocol: TCP
        - name: reindex
          port: 5000
          targetPort: reindex
          protocol: TCP
