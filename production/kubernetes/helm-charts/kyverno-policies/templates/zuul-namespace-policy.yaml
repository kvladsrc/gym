---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: label-zuul-namespace-privileged
  annotations:
    policies.kyverno.io/title: Label Zuul Namespace Privileged
    policies.kyverno.io/category: Security
    policies.kyverno.io/subject: Namespace
    policies.kyverno.io/description: >-
      Add privileged Pod Security Standard labels to the static Zuul namespace
      and the temporary build namespaces created by zuul-launcher.
spec:
  rules:
    - name: label-namespace
      match:
        any:
          - resources:
              kinds:
                - Namespace
              names:
                - zuul
          - resources:
              kinds:
                - Namespace
              selector:
                matchLabels:
                  zuul_kubernetes_kind: pod
      mutate:
        patchStrategicMerge:
          metadata:
            labels:
              pod-security.kubernetes.io/enforce: privileged
              pod-security.kubernetes.io/audit: privileged
              pod-security.kubernetes.io/warn: privileged
