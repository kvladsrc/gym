apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zooreader
  namespace: flux-system
spec:
  install:
    createNamespace: true
  releaseName: zooreader
  targetNamespace: zooreader
  storageNamespace: zooreader
  interval: 10m
  chart:
    spec:
      chart: ./production/kubernetes/helm-charts/zooreader
      sourceRef:
        kind: GitRepository
        name: gerrit
        namespace: flux-system
  values:
    nxs:
      deployments:
        zooreader:
          containers:
            zooreader:
              # yamllint disable-line rule:line-length
              imageTag: prod-1782468604-9c1f580de916a9d20fb940ff207ad3ce1381369f # {"$imagepolicy": "flux-system:zooreader:tag"}
