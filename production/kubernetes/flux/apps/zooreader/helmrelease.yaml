apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zooreader
  namespace: flux-system
spec:
  install:
    createNamespace: true
  releaseName: zooreader
  targetNamespace: zooreader
  storageNamespace: zooreader
  interval: 10m
  chart:
    spec:
      chart: ./production/kubernetes/helm-charts/zooreader
      sourceRef:
        kind: GitRepository
        name: gerrit
        namespace: flux-system
  values:
    nxs:
      deployments:
        zooreader:
          containers:
            zooreader:
              # yamllint disable-line rule:line-length
              imageTag: prod-1781989978-2911937ad7e9049aaa1f71ed78da1bce0991790e # {"$imagepolicy": "flux-system:zooreader:tag"}
