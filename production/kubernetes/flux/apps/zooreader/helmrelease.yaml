apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zooreader
  namespace: flux-system
spec:
  install:
    createNamespace: true
  releaseName: zooreader
  targetNamespace: zooreader
  storageNamespace: zooreader
  interval: 10m
  chart:
    spec:
      chart: ./production/kubernetes/helm-charts/zooreader
      sourceRef:
        kind: GitRepository
        name: gerrit
        namespace: flux-system
  values:
    nxs:
      deployments:
        zooreader:
          containers:
            zooreader:
              # yamllint disable-line rule:line-length
              imageTag: prod-1781779600-ab62d943388a888528fdbf6e8bff3668b1e963e1 # {"$imagepolicy": "flux-system:zooreader:tag"}
