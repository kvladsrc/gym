---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metrics-server
  namespace: flux-system
spec:
  install:
    createNamespace: true
  releaseName: metrics-server
  targetNamespace: kube-system
  storageNamespace: kube-system
  interval: 10m
  chart:
    spec:
      chart: ./production/kubernetes/helm-charts/metrics-server-wrapper
      sourceRef:
        kind: GitRepository
        name: gerrit
        namespace: flux-system
