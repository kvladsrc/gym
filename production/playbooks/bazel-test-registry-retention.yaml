---
- hosts: all
  gather_facts: false

  vars:
    bazel_target: //production/registry-retention:all

  tasks:
    - name: Run registry-retention Bazel tests
      ansible.builtin.include_tasks:
        file: tasks/run-bazel-test-target.yaml
