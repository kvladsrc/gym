---
- name: Reconcile the lab server
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  vars_files:
    - vars/main.yaml
  roles:
    - role: base
    - role: docker
    - role: cockpit
    - role: gitops
