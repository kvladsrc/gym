---
- hosts: all
  gather_facts: false

  vars:
    bazel_target: //cpp/rbtree/...
    bazel_configs:
      - asan
      - ubsan
      - tsan

  tasks:
    - name: Run cpp/rbtree Bazel tests
      ansible.builtin.include_tasks:
        file: tasks/run-bazel-test-target.yaml
