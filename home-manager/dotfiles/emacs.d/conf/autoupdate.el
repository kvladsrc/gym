;;; autoupdate.el --- automatically update Emacs Lisp libraries
;;; Commentary:
;;; Code:

(use-package auto-package-update
  :ensure t
  :config
  (auto-package-update-now))

;;; autoupdate.el ends here
