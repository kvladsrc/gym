# -*- mode: snippet -*-
# name: gcd
# key: gcd
# --
int gcd(int a, int b) {
  if (a == 0) {
    return b;
  }

  if (b == 0) {
    return a;
  }

  if (a % b == 0) {
    return abs(b);
  }

  return gcd(b, a % b);
}
