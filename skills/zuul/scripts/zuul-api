#!/usr/bin/env bash
set -euo pipefail

base_url="${ZUUL_API_URL:-https://ci.your.domain/api}"
path="${1:-/info}"

if [[ $path != /* ]]; then
    path="/$path"
fi

curl -fsS -H "Accept: application/json" "${base_url}${path}"
