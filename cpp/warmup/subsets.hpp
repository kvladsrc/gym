#ifndef CPP_WARMUP_SUBSETS_H_
#define CPP_WARMUP_SUBSETS_H_

#include <vector>

namespace warmup {

std::vector<std::vector<int>> generate_subsets(const std::vector<int> &a);

}  // namespace warmup

#endif  // CPP_WARMUP_SUBSETS_H_
