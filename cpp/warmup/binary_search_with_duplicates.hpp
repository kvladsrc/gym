#ifndef CPP_WARMUP_BINARY_SEARCH_WITH_DUPLICATES_H_
#define CPP_WARMUP_BINARY_SEARCH_WITH_DUPLICATES_H_

#include <cstddef>
#include <optional>
#include <vector>

namespace warmup {
std::optional<std::size_t> lower_bound(const std::vector<int> &a, int value);
}  // namespace warmup

#endif  // CPP_WARMUP_BINARY_SEARCH_WITH_DUPLICATES_H_
