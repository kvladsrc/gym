#ifndef CPP_WARMUP_MEDIAN_OF_ARRAYS_H_
#define CPP_WARMUP_MEDIAN_OF_ARRAYS_H_

#include <vector>

namespace warmup {

int get_elem_by_num(const std::vector<int>& a, size_t num);

int nth_of_arrays(const std::vector<int>& a, const std::vector<int>& b,
                  size_t n);

}  // namespace warmup

#endif  // CPP_WARMUP_MEDIAN_OF_ARRAYS_H_
