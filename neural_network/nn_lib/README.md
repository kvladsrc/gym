# Neural network library

`nn_lib.h` and `nn_lib.cc` contain the small neural network
implementation used by the examples in the parent directory. The code
intentionally uses only the C++ standard library and keeps the forward
pass, backpropagation, initialization, and optimization logic together
for educational use.
